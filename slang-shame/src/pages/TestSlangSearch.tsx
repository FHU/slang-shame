function TestSlangSearch() {
  return (
    <div>
        <h1>This is for using the search component on slang</h1>
        <p>
            A dev should use this page to test functionality between components
            so styling is easier on the other user pages.
            <b />
            This can be not fully functional, but merged to showcase current capabilities.
        </p>
    </div>
  )
}

export default TestSlangSearch