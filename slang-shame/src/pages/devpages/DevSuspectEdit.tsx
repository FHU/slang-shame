function DevSuspectEdit() {
  return (
    <div>
        <h1>
            API call testing for adding and removing suspects
        </h1>
        <p>
            This is a minimal interface for devs to add and remove
            suspects to the Appwrite database table "suspects"
            <b/>
            We could do a "test group" with "test suspects" but currently
            we are using the FHU group for it.
        </p>
    </div>
  )
}

export default DevSuspectEdit